# publisher は移設されました

Pi 側の mic publisher は kk_rescue26_pi リポジトリに移設されました:
https://github.com/KinkiKnights/kk_rescue26_pi (mic_publisher/)

このリポジトリには受信側 (receiver/mic_receiver.py) のみが残ります。
配信フォーマット (FLAC over TCP) を変更する場合は、両リポジトリを同時に更新してください。
